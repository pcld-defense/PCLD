# Pinned dependencies are declared once in pyproject.toml.
# Install the project (and all pins) in editable mode:
#
#   pip install -e .
#
# For a CUDA build of torch on Linux, install torch first from the PyTorch
# index, then the project (pip will keep the already-satisfied pin):
#
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -e .
-e .